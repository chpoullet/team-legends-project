from food_class import *

class Drinks(Food):
    def __init__(self, name, price):
        super().__init__(name, price)
        self.name = price
        self.price = price


















# start a variable to hold all your object (list)

# iterate over dict
    # use the key to pass in the parameter name
    # use the dictionary + key to get the value, and pass in as the parameter price
    # use the obove to create instances of FOOD!
    # add the instance to the variable you initiated outside the iteration


    # def __init__(self, drink_menu):
    #     self.drink_order = []

        # drink1 = FoodItem('water', 3, ['water'])
        # drink2 = FoodItem('Coke', 3, ['others'])
        # drink3 = FoodItem('Smoothies', 4 , ['oranges', 'carrots', 'kiwi'])



